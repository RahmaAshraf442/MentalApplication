<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mental_health_app">

    <application
        android:name=".HiltApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".QuotesActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name="com.example.notesapp.notes.view.NotesActivity"
            android:theme="@style/Theme.NotesApp" />
        <activity
            android:name="com.example.notesapp.notes.view.AddEditNoteActivity"
            android:theme="@style/Theme.NotesApp" />
        <activity
            android:name=".chat.view.ChatActivity"
            android:theme="@style/Theme.NotesApp" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>